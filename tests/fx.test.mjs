import test from 'node:test';import assert from 'node:assert/strict';import {validateBankQuote} from '../public/fx.js';
test('bank FX validates source, spot-buy type and timestamps before applying',()=>{const now=Date.parse('2026-09-22T12:00:00Z'),q={schemaVersion:1,source:'臺灣銀行',rateType:'USD 即期買入',rate:31.645,quotedAt:'2026-09-22T16:00:00+08:00',checkedAt:'2026-09-22T11:50:00Z',status:'ok'};assert.equal(validateBankQuote(q,now).rate,31.645);assert.equal(validateBankQuote(q,now).stale,false);assert.throws(()=>validateBankQuote({...q,rateType:'USD 現金買入'},now));assert.throws(()=>validateBankQuote({...q,rate:null},now));assert.throws(()=>validateBankQuote({...q,quotedAt:'2028-01-01'},now));assert.equal(validateBankQuote({...q,status:'stale'},now).stale,true);assert.equal(validateBankQuote({...q,checkedAt:'2026-09-22T08:00:00Z'},now).stale,true);});
